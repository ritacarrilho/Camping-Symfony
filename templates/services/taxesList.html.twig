<section class="mt-5 mb-5" 
        style="background-color: #00341c; width:100%; height:450px;"> 
    <div class="container">
        <h3 class="mb-2 pt-5 text-white">Our Taxes</h3>

        <div class="d-flex flex-row g-5 justify-content-start gap-5">
            <div class="d-flex flex-column">
            <h5 class="text-white pb-2">{{ taxes[0].label }}</h5>
            {% for tax in taxes %}
                {% if tax.label == 'Pool Tax' %}
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ tax.consumerType }}</h5>
                            <p class="card-text">{{ tax.perDay }}€ </p>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
            </div>

            <div class="d-flex flex-column">
            <h5 class="text-white pb-2">{{ taxes[2].label }}</h5>
            {% for tax in taxes %}
                {% if tax.label == 'Daily Tax' %}
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ tax.consumerType }}</h5>
                            <p class="card-text">{{ tax.perDay }}€</p>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
            </div>
        </div>
    </div>
</section>   

<section class="container mt-5 mb-5"> 
    <p>Don't wast time, book it now and enjoy a good and quiet time with your family, friend or even by yourself!</p>
</section>