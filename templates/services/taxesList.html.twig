    <section class="mt-5 mb-5" style="background-color: #00341c; width:100%; height:450px;"> 

    <div class="container">
                <h3 class="mb-2 pt-5 text-white">Our Taxes</h3>

                <div class="d-flex flex-column g-5 align-items-center">

                     {% for tax in taxes %}
                      {% if tax.label == 'Pool Tax' %}
                        <h5 class="text-white pb-2">{{ tax.label }}</h5>
                        <div class="card" style="width: 18rem;">
                            <div class="card-body">
                                <h5 class="card-title">{{ tax.consumerType }}</h5>
                                <p class="card-text">{{ tax.perDay }}€ </p>
                            </div>
                        </div>

                        {% else %}
                        <h5 class="text-white pb-2">{{ tax.label }}</h5>
                            <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title">{{ tax.consumerType }}</h5>
                                    <p class="card-text">{{ tax.perDay }}€</p>
                                </div>
                            </div>
                        {% endif %}
                        {% endfor %}

                    
                    
                </div>
            </div>
            </div>
    </section>   

        <section class="container mt-5 mb-5"> 
            <p>Don't wast time, book it now and enjoy a good and quiet time with your family, friend or even by yourself!</p>
       </section>